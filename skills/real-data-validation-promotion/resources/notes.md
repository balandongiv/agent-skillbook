# Notes

Use this skill when the user explicitly rejects synthetic smoke tests or when recent failures have shown that the real dataset layout, manual annotations, or editable dependencies matter.

This skill complements:

- `experiment-runbook-discipline` for observable long runs and fresh prefixes
- `intellij-line-debugging` for serial IDE stepping into the real path
- `implementation-aligned-planning` for keeping docs truthful after the validation logic changes
